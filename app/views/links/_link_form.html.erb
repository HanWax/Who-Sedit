<%= form_for @link do |f| %>

	<%= f.label :title %>
	<%= f.text_field :title %>

	<%= f.label :url %>
	<%= f.url_field :url %>

	<%= f.label :comment %>
	<%= f.text_field :comment %>

	<%= f.submit 'Create link' %>

<% end %>